# 泛型

[go 返回值类型能设为可选的吗？a 或 b](../appendices/15.md#go-返回值类型能设为可选的吗a-或-b)

[接口不是规范方法行为的吗？与类型有什么关系？](../appendices/15.md#接口不是规范方法行为的吗与类型有什么关系)

```go
import "fmt"

func add[T int | uint](a, b T) T {
	return a + b
}

fmt.Println(add(2, 3))
```

