# 向量空间与子空间

## 向量空间

[Sympy 有求秩的函数吗？](../appendices/3.1.qmd#sympy-有求秩的函数吗)

[SymPy 对角相关两个核心函数](../appendices/3.1.qmd#sympy-对角相关两个核心函数)

[SymPy 提取上三角 / 严格上三角矩阵函数](../appendices/3.1.qmd#sympy-提取上三角-严格上三角矩阵函数)

> [Sympy 一维扁平列表默认是列](../appendices/3.1.qmd#sympy-一维扁平列表默认是列)

例 4

```{python}
import sympy as sp
from IPython.display import display, Markdown

A = sp.Matrix([
    [1, 0],
    [4, 3],
    [2, 3],
])

b = sp.Matrix(0.4 * sp.Matrix([1, 4, 2]) + 0.3 * sp.Matrix([0, 3, 3]))
display(b)  # (0.4, 2,5, 1.7)

display(A.solve(b))
```

[那当 m<n 的时候，怎么理解这个事情](../appendices/3.1.qmd#那当-mn-的时候怎么理解这个事情)

[三维之内的还比较好理解，每个维度都是垂直的，三维以上的呢？维度也是垂直的吗？](../appendices/3.1.qmd#三维之内的还比较好理解每个维度都是垂直的三维以上的呢维度也是垂直的吗)

[就是 n 维空间的坐标轴是垂直的，向量间不一定是垂直的？](../appendices/3.1.qmd#就是-n-维空间的坐标轴是垂直的向量间不一定是垂直的)

[维度真正依赖的是线性无关怎么理解？](../appendices/3.1.qmd#维度真正依赖的是线性无关怎么理解)

[线性无关的英语是什么，我觉得这可能是卡点](../appendices/3.1.qmd#线性无关的英语是什么我觉得这可能是卡点)

[这在第三章需要理解到什么程度可以往后？](../appendices/3.1.qmd#这在第三章需要理解到什么程度可以往后)

[$Ax=0$ 存在非零解说明什么？说明 $A$ 的列线性相关。这个好像不太理解](../appendices/3.1.qmd#ax0-存在非零解说明什么说明-a-的列线性相关这个好像不太理解)

[那解线性方程组相当于找出那个线性相关的关系来？](../appendices/3.1.qmd#那解线性方程组相当于找出那个线性相关的关系来)

[$b\neq0$的话，那 Ax 的线性组合还叫线性相关吗？](../appendices/3.1.qmd#bneq0的话那-ax-的线性组合还叫线性相关吗)

例 5

```{python}
from sympy import Matrix
from IPython.display import display, Markdown

B = Matrix([
    [1, 2, 3],
    [0, 0, 4],
])

b = Matrix([5, 4])

display(Markdown(f"rank = ${B.rank()}$"))
# display(Markdown(f"nullspace = ${latex(B.nullspace())}$"))
display(Markdown("<br>nullspace = "))
display(B.nullspace())
# display(B.solve(b))  # 它只用于方阵求解
display(Markdown("\n\ngauss_jordan_solve = "))
display(B.gauss_jordan_solve(b))
display(Markdown("  \nrref = "))
display(B.rref())
```

[Sympy 中有没有看线性相关的函数？](../appendices/3.1.qmd#sympy-中有没有看线性相关的函数)

[A.nullspace() 算出来的就是线性相关的系数啊，就是当 b=0 的时候方程组的解？](../appendices/3.1.qmd#anullspace-算出来的就是线性相关的系数啊就是当-b0-的时候方程组的解)

[0 空间不可能有唯一值，要么无解，要么多解？](../appendices/3.1.qmd#0-空间不可能有唯一值要么无解要么多解)

[噢，0 空间不可能有唯一非 0 解，也不可能无解；要么唯一解是 0，要么无穷多解](../appendices/3.1.qmd#噢0-空间不可能有唯一非-0-解也不可能无解要么唯一解是-0要么无穷多解)

[m<n 时肯定有线性相关的列？](../appendices/3.1.qmd#mn-时肯定有线性相关的列)

[display(B.solve(b)) 报错](../appendices/3.1.qmd#displaybsolveb-报错)

[特解是什么，每个人算的特解不一样吗？](../appendices/3.1.qmd#特解是什么每个人算的特解不一样吗)

## A 的零空间：求解 Ax = 0 与 Rx = 0

```{python}
from sympy import Matrix
from IPython.display import display, Markdown

A = Matrix([
    [1, 0],
    [0, 1],
])
B = Matrix([
    [1, 2],
    [0, 0],
])
display(Markdown("rank:"))
display(A.rank(), B.rank())
display(Markdown("nullspace:"))
display("A", *A.nullspace(), "B", *B.nullspace())
display(Markdown("upper_triangular:"))
display(A.upper_triangular(), B.upper_triangular())
display(Markdown("LUdecomposition:"))
display("A", *A.LUdecomposition(), "B", *B.LUdecomposition())
display(Markdown("rref"))
display("A", *A.rref(), "B", *B.rref())
```

[当各列都不相关的时候，方程组=0 ~~无解~~ 只有零解，0 空间只有 0？](../appendices/3.2.qmd#当各列都不相关的时候方程组0-无解0-空间只有-0)

例 1

```{python}
from sympy import Matrix
from IPython.display import display, Markdown

A = Matrix([
    [1, 2],
    [3, 6],
])

display(Markdown("det:"))
display(A.det())
display(Markdown("rref:"))
display(*A.rref())
display(Markdown("特殊解："))
display(*A.nullspace())
display(Markdown("通解："))
display(*A.gauss_jordan_solve(Matrix.zeros(A.rows, 1)))
```

[特殊解](../appendices/3.2.qmd#特殊解)

例 2

```{python}
from sympy import Matrix
from IPython.display import display, Markdown

A = Matrix([[1, 2, 3]])

display(A.rank())
display(A.nullspace())
display(A.gauss_jordan_solve(Matrix.zeros(1, 1)))
display(A.gauss_jordan_solve(Matrix.zeros(1, 1), freevar=True))  # 可以看自由列
display(A.rref())
```

[0 空间的维度](../appendices/3.2.qmd#0-空间的维度)

[这里面是不是只有 rref 能看自由列是哪一列？](../appendices/3.2.qmd#这里面是不是只有-rref-能看自由列是哪一列)

[x+2y+3z = 6 怎么求它的解？](../appendices/3.2.qmd#x2y3z-6-怎么求它的解)

[计算特殊解](../appendices/3.2.qmd#计算特殊解)

例 3

```{python}
from sympy import Matrix
from IPython.display import display, Markdown

# 定义A
A = Matrix([
    [1, 2],
    [3, 8],
])

# B = [A; 2A] 纵向上下拼接（vstack）
B = Matrix.vstack(A, 2 * A)

# C = [A 2A] 横向左右拼接（hstack）
C = Matrix.hstack(A, 2 * A)

# 打印查看
display("A =", A)
display("B =", B)
display("C =", C)

display(Markdown("rref:"))
display(*A.rref(), *B.rref(), *C.rref())

display(Markdown("nullspace:"))
display(*A.nullspace(), *B.nullspace(), *C.nullspace())

display(Markdown("gauss_jordan_solve:"))
for M in (A, B, C):
    display(*M.gauss_jordan_solve(Matrix.zeros(M.rows, 1), freevar=True))
```

[特解（particular solution）与特殊解（special solution）](../appendices/3.2.qmd#特解particular-solution与特殊解special-solution)

```{python}
display("C", C)
display("U", *C.echelon_form(with_pivots=True))
display("R", *C.rref())
```

### 矩阵的秩

```{python}
from sympy import Matrix
from IPython.display import display, Markdown

A = Matrix([[1, 1, 2, 4], [1, 2, 2, 5], [1, 3, 2, 6]])
R = A.rref()
display(A)
display(*R)
display(*A.nullspace())
display(*A.gauss_jordan_solve(Matrix.zeros(A.rows, 1)))
```

[每个“自由列“都是前面主元列的线性组合，而特殊解 s 中包含这些线性组合的系数：是什么意思？](../appendices/3.2.qmd#每个自由列都是前面主元列的线性组合而特殊解-s-中包含这些线性组合的系数是什么意思)

[`[-2,0,1,0]`里的`[1,0]`是谁的取值？](../appendices/3.2.qmd#2010里的10是谁的取值)

```{python}
from sympy import Matrix, symbols, solve
from IPython.display import display, Markdown

x1, x2, x3, x4 = symbols("x1,x2,x3,x4")
print(solve(x1 + 2 * x3 + 3 * x4, x3))
```

[`A = Matrix([[1, 1, 2, 4], [1, 2, 2, 5], [1, 3, 2, 6]])`用这个举例说明组合系数](../appendices/3.2.qmd#a-matrix1-1-2-4-1-2-2-5-1-3-2-6用这个举例说明组合系数)

### 秩 1 矩阵

```{python}
from sympy import Matrix
from IPython.display import display, Markdown

A = Matrix([
    [1, 3, 10],
    [2, 6, 20],
    [3, 9, 30],
])
display("A", A)

R, _ = A.rref()
display("R", R)

u = A[:, 0]
display("u", u)
vT = A[0, :]  # vT 只是恰好能用，实际是需要计算，见附录
display("vT", vT)

B = u * vT
display("b", B)
display("A == B", A == B)
```

[vT 只是恰好能用，实际是需要找的？那直接看出来的还是可以用 sympy 计算出来的？](../appendices/3.2.qmd#vt-只是恰好能用实际是需要找的那直接看出来的还是可以用-sympy-计算出来的)

[u 为什么叫方向，它不只是一个点吗？](../appendices/3.2.qmd#u-为什么叫方向它不只是一个点吗)
<<<<<<< HEAD

[dim](../appendices/4.md#dim)

=======
>>>>>>> 3c327d1dea06c64dbd66727994063946668a5116
