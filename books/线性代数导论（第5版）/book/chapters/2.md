# 线性方程组的求解

## 向量与线性方程组

1. 线性方程组 $Ax = b$ 的列图：从列向量的视角解读线性方程组，即矩阵 $A$ 的 $n$ 个列向量的线性组合生成向量 $b$.
2. $Ax = x_1\boldsymbol{a}_1 + x_2\boldsymbol{a}_2 + \dots + x_n\boldsymbol{a}_n = \boldsymbol{b}$ 是向量方程：$\boldsymbol{a}_1,\boldsymbol{a}_2,\dots,\boldsymbol{a}_n$ 是矩阵 $A$ 的列向量。
3. 当 $\boldsymbol{b} = \boldsymbol{0}$ 时，矩阵 $A$ 的列向量组的线性组合 $Ax$ 是零向量：一种可能的情况是 $\boldsymbol{x} = (0,0,\dots,0)$.
4. 线性方程组 $Ax = b$ 的行图：从行向量的视角解读线性方程组，即由 $m$ 行方程确定的 $m$ 个平面相交于点 $\boldsymbol{x}$.
5. 上述每个平面的方程都可由点积表示：
   (第 1 行)$\cdot \boldsymbol{x} = b_1$, (第 2 行)$\cdot \boldsymbol{x} = b_2$, $\dots$, (第 $m$ 行)$\cdot \boldsymbol{x} = b_m$.
6. 当 $\boldsymbol{b} = \boldsymbol{0}$ 时，所有平面 (第 $i$ 行)$\cdot \boldsymbol{x} = 0$ 都经过原点 $\boldsymbol{x} = (0,0,\dots,0)$.

$$Ax=b$$

```python
import sympy as sp

A = sp.Matrix([[1, -2], [3, 2]])
b = sp.Matrix([1, 11])
x = A.solve(b)
# 数值计算库（SciPy、MATLAB）：全部用 solve(A,b)，贴合工程师、统计人解方程习惯；
# SymPy 符号矩阵：选用 A.solve(b)，偏向抽象代数、变换映射的 OO 设计；
# 两者只是设计取舍，不存在“谁数学上更标准”。
print(x)
# print(sp.solve(A, b)) # 输出 []
```

```python
x1, x2 = sp.symbols("x1 x2")
sp.solve([x1 - 2 * x2 - 1, 3 * x1 + 2 * x2 - 11], [x1, x2])
# 语义：通用解方程函数，传方程列表、变量列表
```

例 1

```python
import sympy as sp

A = sp.Matrix([[1, 0, 0], [1, 0, 0], [1, 0, 0]])
print(A)
b = sp.Matrix([4, 5, 6])
print(A @ b)
print(A * b)
```

```python
I = sp.eye(3)
print(I)
x = sp.Matrix([4, 5, 6])
print(I @ x)
```

## 矩阵运算的规则

### 习题

#### 23

```python
import sympy as sp

A = sp.Matrix([
    [1, 2],
    [3, 4],
])
x = sp.Matrix([5, -2])
b = sp.Matrix([1, 7])

sp.pprint(A)
sp.pprint(x)
sp.pprint(b)
print(A @ x == b)
print(A * x == b)
```

```python
# 行向量与列向量的区别
import sympy as sp

# 列向量
v_col = sp.Matrix([1, 2, 3])
sp.pprint(v_col)
print(v_col.shape)
# 行向量
v_row = sp.Matrix([
    [1, 2, 3],
])
sp.pprint(v_row)
print(v_row.shape)
```

#### 24

```python
import sympy as sp

A = sp.eye(3)
v = sp.Matrix(list(range(3, 6)))
sp.pprint(A)
sp.pprint(v)
sp.pprint(v.T)
sp.pprint(A * v)
# sp.pprint(v @ v)  # ShapeError: Matrix size mismatch: (3, 1) * (3, 1).
sp.pprint(v.T * v)  # 行 * 列：点积
print(sp.simplify(sp.sqrt(v.T * v)[0, 0]))
print(v.norm())
sp.pprint(v * v.T)  # 列 * 行：外积
sp.pprint(v.T * A)
# sp.pprint(v * A)  # ShapeError: Matrix size mismatch: (3, 1) * (3, 3).
```

#### 25

```python
import sympy as sp

A = sp.ones(4)
sp.pprint(A)
v = sp.ones(4, 1)
sp.pprint(v)
sp.pprint(A * v)
```

```python
import sympy as sp

B = sp.eye(4) + sp.ones(4)
sp.pprint(B)
w = sp.zeros(4, 1) + 2 * sp.ones(4, 1)
sp.pprint(w)
sp.pprint(B * w)
```
